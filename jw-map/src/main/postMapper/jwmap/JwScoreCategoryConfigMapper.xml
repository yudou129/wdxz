<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.jwmap.mapper.JwScoreCategoryConfigMapper">

    <resultMap id="BaseResultMap" type="com.ruoyi.jwmap.domain.JwScoreCategoryConfig">
        <id property="configId" column="config_id"/>
        <result property="categoryCode" column="category_code"/>
        <result property="categoryName" column="category_name"/>
        <result property="indicatorCode" column="indicator_code"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="isActive" column="is_active"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectAllActive" resultMap="BaseResultMap">
        SELECT * FROM jw_score_category_config
        WHERE is_active = '1'
        ORDER BY category_code, sort_order
    </select>

    <select id="selectByCategory" resultMap="BaseResultMap">
        SELECT * FROM jw_score_category_config
        WHERE category_code = #{categoryCode} AND is_active = '1'
        ORDER BY sort_order
    </select>

    <select id="selectDistinctCategories" resultType="string">
        SELECT DISTINCT category_code FROM jw_score_category_config
        WHERE is_active = '1'
        ORDER BY category_code
    </select>

    <insert id="insertCategoryConfig" parameterType="com.ruoyi.jwmap.domain.JwScoreCategoryConfig"
            useGeneratedKeys="true" keyProperty="configId">
        INSERT INTO jw_score_category_config (
            category_code, category_name, indicator_code, sort_order, is_active,
            create_time, update_time
        ) VALUES (
            #{categoryCode}, #{categoryName}, #{indicatorCode}, #{sortOrder}, #{isActive},
            now(), now()
        )
    </insert>

    <update id="updateCategoryConfig" parameterType="com.ruoyi.jwmap.domain.JwScoreCategoryConfig">
        UPDATE jw_score_category_config
        <set>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="indicatorCode != null">indicator_code = #{indicatorCode},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
            update_time = now()
        </set>
        WHERE config_id = #{configId}
    </update>

    <delete id="deleteByCategory">
        DELETE FROM jw_score_category_config WHERE category_code = #{categoryCode}
    </delete>

    <delete id="deleteByIndicatorCode">DELETE FROM jw_score_category_config WHERE indicator_code = #{indicatorCode}</delete>
    <update id="updateIndicatorCode">UPDATE jw_score_category_config SET indicator_code = #{newCode} WHERE indicator_code = #{oldCode}</update>

</mapper>
